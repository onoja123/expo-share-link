<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <provider
      android:name="expo.modules.sharelink.ShareLinkFileProvider"
      android:authorities="${applicationId}.ExpoShareLinkFileProvider"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/expo_share_link_file_paths" />
    </provider>
  </application>
</manifest>
